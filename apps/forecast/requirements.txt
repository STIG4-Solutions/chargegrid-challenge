# Nucleo do pipeline de previsao.
#
# VERSOES EXATAS, e nao pisos, por uma razao que os pisos nao atendem: o
# artefato treinado precisa ser REPRODUZIVEL. Com `lightgbm>=4.0`, dois clones
# construidos com meses de diferenca resolvem versoes diferentes, treinam
# arvores diferentes e produzem numeros diferentes - e as metricas publicadas
# no README deixam de ser conferiveis por quem quer que seja.
#
# Ha um segundo motivo, mais concreto: o artefato e' um pickle de objetos
# `LGBMRegressor`. Pickle nao e' formato de intercambio - ele carrega o grafo de
# objetos daquela versao da biblioteca. Um `modelo_atual.joblib` gravado com
# lightgbm 4.7 e lido com 5.x avisa, falha ou, pior, carrega errado.
#
# Para SUBIR uma versao: altere aqui, reconstrua a imagem, rode `treinar.py` e
# compare `modelos/metricas_atual.json` com o commit anterior. A mudanca nas
# metricas E' a revisao dessa atualizacao.
pandas==3.0.5
numpy==2.5.3
lightgbm==4.7.0
# Exigido por `lightgbm.sklearn` em tempo de execucao. Nao estava no
# requirements do repositorio de modelagem - devia vir de outro pacote no
# ambiente de quem o escreveu, e sem ele o treino quebra no backtest.
scikit-learn==1.9.0
joblib==1.6.0

# Leitura do banco do ChargeGrid. Driver SINCRONO de proposito: o pipeline e'
# pandas, e pandas nao fala asyncpg - que e' o driver da API.
SQLAlchemy==2.0.52
psycopg[binary]==3.3.5

# Testes do pipeline (apps/forecast/tests). Fora do pytest da API de proposito:
# lightgbm e numpy nao entram na imagem que roda o rebalanceamento de potencia.
pytest==8.4.2

# NAO estao aqui, e o motivo importa:
#   pymodbus   - a camada Modbus do pipeline original tem enderecos de
#                registrador inventados; os dados vem do banco, nao do medidor.
#   pyarrow    - so' seria preciso para ler/gravar parquet, e o painel vem do
#                Postgres direto.
#   matplotlib - graficos opcionais do backtest.
